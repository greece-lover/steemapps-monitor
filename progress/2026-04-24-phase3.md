# Phase 3 — Monitor-Kern-Implementierung

**Datum:** 2026-04-24
**Phase:** 3
**Status:** abgeschlossen

## Ziel

Ein funktionsfähiger, lokaler Monitor auf `/opt/steemapps-monitor/` der VM,
der alle 60 Sekunden die vier konfigurierten Steem-API-Nodes abfragt,
Messwerte in SQLite persistiert und einen lokalen HTTP-Endpoint auf
`127.0.0.1:8110` bereitstellt.

## VM-Unterbrechung während der Session

Zwischen Code-Fertigstellung und Deploy war die VM eine Weile offline
(weder Ping noch SSH antworteten; keine Retry-Schleife, wie im Briefing
festgelegt). Nach dem Wieder-Hochfahren ging der Deploy in einem Rutsch
durch. Zwei kleine Stolpersteine beim Deploy — beide im Abschnitt
„Deploy-Details" unten dokumentiert.

## Ergebnisse (lokal, committet)

### Neue Module im Repo-Root

Flaches Layout, keine Package-Struktur — die Modul-Menge ist klein und
stabil genug, dass ein zusätzliches Verzeichnis nur Zeremonie wäre.

- `monitor.py` — Entry-Point. Startet asyncio-Event-Loop mit zwei Tasks:
  der Poll-Schleife und einem eingebetteten uvicorn-Server. Die
  Poll-Schleife feuert sofort bei Boot einen ersten Tick (damit die API
  nicht erst nach einer Minute Daten hat), dann alle 60 s. Signal-Handler
  für SIGINT/SIGTERM, damit systemd sauber stoppen kann.
- `database.py` — SQLite-Schicht. `measurements`- und `nodes`-Tabelle mit
  Indexen auf `timestamp`, `node_url` und dem zusammengesetzten
  `(node_url, timestamp)`. WAL-Modus für den Ein-Schreiber-Viele-Leser-
  Pattern. Funktionen: `initialise`, `sync_nodes`, `insert_measurement`,
  `get_recent_measurements`, `get_latest_per_node`, `get_uptime_stats`,
  `row_count`.
- `scoring.py` — reine Score-Berechnung, keine I/O. Setzt die
  `mv1`-Regeln aus `MEASUREMENT-METHODOLOGY.md` 1:1 um: Latenz-Bänder
  (>500 ms, >2000 ms), Block-Lag-Bänder (>3, >10), Error-Rate >20 %
  über letzte 20 Ticks, kein Response in diesem Tick. Penalty-Konstanten
  sind modul-level und matchen das Konzept. Scores werden bei 0 gekappt.
- `api.py` — FastAPI-App. Drei Routen unter `/api/v1/`: `health`,
  `status`, `nodes/{url}/history`. `status` verknüpft den letzten Tick pro
  Node mit dem Score und einer menschenlesbaren `reasons`-Liste
  (genau die Penalty-Labels, die der Score angewendet hat).
- `logger.py` — stdout-Logging mit ISO-Zeitstempel für systemd-Journal.
  Idempotentes `setup()`.
- `config.py` — Pfade, Intervalle, `load_nodes()`-Helper. `SOURCE_LOCATION`
  kommt aus `STEEMAPPS_SOURCE_LOCATION` env var (Default
  `contabo-de-1`), damit später Multi-Location-Betrieb ohne Code-Änderung
  geht.
- `nodes.json` — die vier bestätigten Nodes aus Phase 1:
  `api.steemit.com`, `api.justyy.com`, `api.steem.fans`, `api.steemyy.com`.
  Einzige Wahrheitsquelle; `sync_nodes` übernimmt Upsert + Markierung
  entfernter Nodes als inaktiv.

### systemd-Unit

`deploy/steemapps-monitor.service` mit User `holger`, WorkingDirectory
`/opt/steemapps-monitor`, `Restart=always`, `After=network-online.target`
und einem robusten Hardening-Block:

- `NoNewPrivileges`, `PrivateTmp`, `ProtectSystem=strict`, `ProtectHome`,
  `ReadWritePaths=` nur `data/` und `logs/`
- `RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX`, keine raw sockets
- `MemoryDenyWriteExecute`, `LockPersonality`

`StandardOutput=journal` macht `journalctl -u steemapps-monitor` zum
One-Stop-Logbuch.

### Test-Suite (pytest, 18 Tests, alle grün)

- `tests/test_scoring.py` — 11 Tests. Für jede Scoring-Regel ein Szenario:
  gesunder Node, langsame Latenz einzeln und kumulativ mit sehr-langsam,
  Block-Lag einzeln und kumulativ, „kein Response", hohe Fehlerrate, exakt
  20 % (Grenzfall), Score-Boden bei 0, fehlende Referenz, Status-Label-
  Mapping.
- `tests/test_database.py` — 7 Tests. Idempotente Init, Round-Trip,
  Filter nach Node, Latest-per-Node, Uptime-Statistik, `row_count`,
  `sync_nodes` markiert entfernte Nodes als inaktiv.
- `tests/smoke_one_tick.py` — kein pytest-Test, sondern manueller
  Live-Check gegen die echten Nodes. Ergebnis heute 17:33:

  ```
  api.steemit.com        ok=True  lat=523ms  block=105471530
  api.justyy.com         ok=True  lat=397ms  block=105471530
  api.steem.fans         ok=True  lat=629ms  block=105471530
  api.steemyy.com        ok=True  lat=394ms  block=105471530
  ```

  Alle vier Nodes synchron auf Block 105 471 530, keine Outliers.

### Doku

- `docs/ARCHITECTURE.md` + `docs/ARCHITEKTUR.md` — Abschnitt „Processes /
  Prozesse" aktualisiert (Phase 3: ein Prozess statt zwei), neuer
  Abschnitt „Module layout / Modul-Layout" mit Datei-Tabelle.
- `docs/API.md` — Phase-3-Endpoints (`/api/v1/health`, `/api/v1/status`,
  `/api/v1/nodes/{url}/history`) mit Beispiel-JSON ergänzt; der Phase-4-
  Zielzustand (`/nodes`, `/outages`, `/reports/daily`) bleibt separat
  gelistet, damit externe Konsumenten sich vorbereiten können.
- `deploy/README.md` — Install-, Update-, Log- und Shutdown-Kommandos
  für die VM.

### Python-Abhängigkeiten

- `httpx`, `fastapi`, `uvicorn`, `pydantic` in `requirements.txt`.
  Versions-Constraints als Ranges (`>=X,<Y`), damit Python 3.12 auf der
  VM und Python 3.14 beim lokalen Vor-Test dieselben Dateien nutzen
  können.
- `pytest`, `pytest-asyncio` separat in `requirements-dev.txt`.

## Bewusste Design-Entscheidungen

- **Poller und API in einem Prozess, nicht zwei.** Für Phase 3 ist die
  zusätzliche Inter-Prozess-Koordination (DB-Mutex, Cross-Process-SIGHUP)
  nicht gerechtfertigt. Wenn nginx in Phase 4 dazukommt, wird die API in
  eine eigene Unit ausgegliedert — dann ist es aber auch lohnend.
- **FastAPI-Routen unter `/api/v1/`.** Das Konzept-Dokument schlägt die
  Pfade ohne Prefix vor (`/status`, `/nodes/…`); der Prefix macht aber
  das spätere Versionieren von Schema-Änderungen ohne Breaking-Change
  einfacher — und nginx-Rewrite-Regeln für Phase 4 werden trivialer,
  wenn das API-Root klar abgegrenzt ist.
- **Block-Lag-Referenz = `max(head_block)` über **erfolgreiche** letzte
  Ticks aller Nodes.** Wenn ein Node fehlschlägt, zählt seine NULL nicht
  als „Referenz 0". Das matcht die Methodik-Doku und verhindert, dass ein
  einzelner Ausfall die Lag-Bewertung aller anderen kippt.
- **`source_location` in jeder Zeile, default `contabo-de-1`.** Kostet
  heute nur eine Spalte und macht Phase-5-Multi-Location-Auswertung
  trivial: einfach Messungen aus verschiedenen Instanzen in dieselbe DB
  schreiben (oder per-Instance-DBs mergen, je nach Entscheidung).
- **`sync_nodes` markiert entfernte Nodes als `active=0`, löscht nicht.**
  Historische Messungen bleiben joinable. Wenn ein Node später wieder
  reinkommt, flippt das Flag zurück — Null Datenverlust.
- **`/api/v1/status` liefert `reasons` direkt mit.** Das ist keine interne
  Debug-Info, sondern Teil des Methodik-Versprechens „jede Zahl ist
  reproduzierbar": der User sieht genau, welche Regel welche Punkte
  abgezogen hat.
- **Keine externe Anbindung in Phase 3.** Port 8110 nur auf 127.0.0.1,
  keine UFW-Änderung, keine nginx-Regel. Das passt zur Tabu-Liste
  (keine Fremdeingriffe in bestehende Dienste) und vermeidet Angriffs-
  fläche während der Beta.

## Deploy-Details (auf der VM)

### Transport: `git archive`-Pipe statt `git clone`

Das Repo ist privat und auf der VM war weder ein GitHub-SSH-Key noch das
`gh`-CLI eingerichtet. Statt das jetzt nachzuziehen (würde manuelle
GitHub-Aktion erfordern), wurde die committete HEAD-Version per
`git archive --format=tar HEAD | ssh steemfork "cd /opt/steemapps-monitor && tar xf -"`
rübergeschickt. Clean: nur getrackte Dateien, kein `.git`, keine
gitignoreten Artefakte. Nachteil: Für spätere Updates muss entweder
dieselbe Pipe wiederholt oder ein Deploy-Key eingerichtet werden. Der
Deploy-Key ist der bessere Langfrist-Weg — kommt mit dem ersten Phase-4-
Update.

### systemd-NAMESPACE-Fehler beim ersten Start

Der erste `systemctl start` scheiterte mit
`status=226/NAMESPACE` — weil das Unit-File `ReadWritePaths=data/ logs/`
deklariert, die Verzeichnisse aber noch nicht existierten. Fix:
`mkdir -p /opt/steemapps-monitor/data /opt/steemapps-monitor/logs`,
danach startet der Service sauber. Könnte man über `ExecStartPre` im
Unit-File lösen, aber `deploy/README.md` erwähnt das `mkdir` schon als
Teil der Installation — also eher ein „Anleitung lesen"-Thema als ein
Unit-Bug.

### `sqlite3`-CLI nicht installiert

Auf der VM fehlt das CLI `sqlite3`. Das Python-Modul ist selbstverständlich
da (ist built-in), also stört das den Service nicht; für Ad-hoc-Queries
aus der Shell ist es aber unpraktisch. Workaround für diese Session:
Queries via `.venv/bin/python3 -c "import sqlite3; …"`. Installation des
CLI (`apt install sqlite3`) wird in Phase 4 zusammen mit nginx
nachgezogen — beides Paket-Installationen, die sich bündeln lassen.

## Live-Laufzeit: 30 Minuten, Start 2026-04-24 15:34:30 UTC

### systemd

```
● steemapps-monitor.service - steemapps.com Steem API node monitor (Phase 3)
     Active: active (running) since Fri 2026-04-24 15:34:30 UTC; 30min ago
   Main PID: 5232 (python)
     Memory: 36.2M (peak: 37.7M)
        CPU: 6.591s
```

Null Restarts, RAM pendelt um 36 MB, 6.6 s CPU über 30 min (≈ 3.7 % avg —
lässt sich noch deutlich einsparen durch weniger Detail-Logging, aber
völlig unkritisch).

### DB-Stand nach 31 Ticks

```
total_measurements: 124

node                           ticks  ok    avg   min   max
https://api.justyy.com            31  31    491   460   631
https://api.steem.fans            31  31    617   349   736
https://api.steemit.com           31  31    704   655   799
https://api.steemyy.com           31  31    489   335   782
```

**100 % Uptime über alle vier Nodes in den ersten 30 Minuten.** Die
Latenz-Charakteristik deckt sich mit dem, was der Phase-1-Probe gemessen
hat — api.steemyy.com und api.justyy.com sind die schnellsten, api.steemit.com
der langsamste. Die Spikes (api.steemyy.com max 782, api.steem.fans min 349)
sind in der Range, die ich für eine 60-s-Poll-Kadenz erwartet hätte.

### API-Antwort

`curl -s http://127.0.0.1:8110/api/v1/status`:

```
ref_block: 105473354
  https://api.steemit.com        status=ok        score= 80 lat=662ms lag=0
  https://api.justyy.com         status=ok        score=100 lat=480ms lag=0
  https://api.steem.fans         status=ok        score= 80 lat=661ms lag=0
  https://api.steemyy.com        status=ok        score=100 lat=396ms lag=0
```

**Scoring greift genau nach Spec:** justyy.com (480 ms) und steemyy.com
(396 ms) bleiben unter der 500-ms-Schwelle und bekommen die vollen 100
Punkte; steemit.com (662 ms) und steem.fans (661 ms) kassieren
−20 („latency > 500 ms"), landen bei 80 — immer noch `ok` (≥80). Block-
Lag 0 über alle Nodes bedeutet die Chain-Propagation läuft sauber.

## Commit-Hashes

- `83a3b7d` — Phase-3-Kern (Code, Tests, Unit, Doku)
- `3a81160` — README+CHANGELOG+ROADMAP-Updates
- dieser Abschluss-Commit — Progress-Log-Finalisierung

## Offene Punkte → Phase 4 (öffentliche Statistik-Webseite)

- nginx-Reverse-Proxy für `api.steemapps.com`
- Dashboard-Frontend (statisches HTML + Live-Fetch gegen
  `/api/v1/status`)
- Rate-Limiting auf nginx-Ebene
- CORS-Policy für `steemapps.com`-Subdomains
- Poller und API in getrennte systemd-Units splitten

## Offene Punkte → Phase 5 (Multi-Location)

- Zweite Monitor-Instanz auf IONOS, dritte ggf. USA-VPS
- `source_location` wird pro Instanz via env var gesetzt
- Entscheidung: separate DBs mit Sync-Job vs. eine zentrale DB mit
  push-basiertem Schreiben

## Offene Punkte → Phase 6 (Chain-Reporting)

- Reporter-Account (`@steem-api-health` oder `@greece-lover`?) und
  Posting-Key-Handling (via SARH-crypto-Library oder native beem?)
- Tagesreport-Template (HTML für Post, `custom_json` für Chain-Archiv)
- Cron `@daily 02:00`
